server:
  port: 7890

management:
  metrics:
    web:
      server:
        auto-time-requests: false # 关闭自动检测

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://
    username: nacos
    password: 123456
    druid:
      initial-size: 10
      min-idle: 10
      max-active: 30
      max-wait: 50000

zipkin:
  storage:
    type: mysql # 配置zipkin存储介质为mysql
