CREATE TABLE `users` (
                            `id`  int NOT NULL AUTO_INCREMENT ,
                            `user_name`  varchar(32) NULL ,
                            `password`  varchar(32) NULL ,
                            PRIMARY KEY (`id`)
)
;

