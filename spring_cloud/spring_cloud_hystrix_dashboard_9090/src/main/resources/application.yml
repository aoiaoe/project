server:
  port: 9090
spring:
  application:
    name: dashboard
# 暴露所有actuator端点
management:
  endpoint:
    web:
      expose:
        include: "*"
    health:
      show-details: always

hystrix:
  dashboard:
    proxy-stream-allow-list: "*"
